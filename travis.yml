language: python
python:
  - "3.8"
  
before_install:
  - gem install bundler
  
# command to install dependencies
install:
  - pip install -r requirements.txt
  
# command to run tests
script:
  - pytest
